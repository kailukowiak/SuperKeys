name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate karabiner.json
        run: jq empty mac/karabiner.json

      - name: Cross-platform parity check
        run: python3 scripts/check_parity.py

      - name: ShellCheck
        run: shellcheck -x install update bootstrap.sh linux/*.sh mac/*.sh

      - name: PSScriptAnalyzer
        shell: pwsh
        run: |
          Install-Module PSScriptAnalyzer -Force -Scope CurrentUser
          $results = Invoke-ScriptAnalyzer -Path . -Recurse -Severity Error
          $results | Format-Table -AutoSize
          if ($results) { exit 1 }
